function solution(n, m, section) {
  let answer = 0;
  let painted = 0;
  section.forEach((n) => {
      if(n > part) {
          part = n + m -1 ;
          painted++;
      }
  });
  return painted;
}